import 'package:flutter/material.dart';

class MoodBottomBar extends StatelessWidget {
  const MoodBottomBar({super.key});

  @override
  Widget build(BuildContext context) {
    return BottomNavigationBar(
      items: [],
    );
  }
}
